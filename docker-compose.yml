version: '2'

services:
  canoe-beton:
    build: .
    image: clubcedille/canoe-beton:latest
    container_name: canoe-beton
    ports:
      - 3000:3000
    restart: always
    environment:
      NODE_APP: prod
